UserParameter=vfs.fs.read.ops[*],     dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$4}'
UserParameter=vfs.fs.read.merged[*],  dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$5}'
UserParameter=vfs.fs.read.sectors[*], dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$6}'
UserParameter=vfs.fs.read.ms[*],      dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$7}'
UserParameter=vfs.fs.write.ops[*],    dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$8}'
UserParameter=vfs.fs.write.merged[*], dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$9}'
UserParameter=vfs.fs.write.sectors[*],dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$10}'
UserParameter=vfs.fs.write.ms[*],     dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$11}'
UserParameter=vfs.fs.io.active[*],    dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$12}'
UserParameter=vfs.fs.io.ms[*],        dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$13}'
UserParameter=vfs.fs.io.weight.ms[*], dev=$(mount | grep "$1 " | awk '{print $$1}' | awk -F '/' '{print $$3}') && cat /proc/diskstats | grep $dev | head -1 | awk '{print $$14}'
